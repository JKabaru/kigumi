# Research loop log

Append-only log of every research loop iteration. Never delete entries.

Format:
```
[datetime] PROPOSE: [one sentence]
[datetime] RESULT: [metric] = [value] (baseline = [value])
[datetime] KEEP or REVERT: [one sentence reason if revert]
```

---
_Loop log begins on first iteration_
